language: node_js
node_js:
  - 15
env:
  - LANG=en_US.utf8 LIMIT_JEST=yes CI=yes TZ=America/New_York
script:
  - npm run build
  - npm run format-check
  - npm run test
  - npm run site
  - bash <(curl -s https://codecov.io/bash)